# T1 实施计划

- **日期**：2026-07-29
- **阶段**：T1 经济底层 + 骚扰 + SH1（启动）
- **关联文档**：
  - 设计方案：[T1经济底层改造方案.md](T1经济底层改造方案.md)
  - 原路线图：[T1实施路线图.md](T1实施路线图.md)（部分被本文档替代）
  - 设计纪要：[2026-07-28_玩法推进纪要.md](../sessions/2026-07-28_玩法推进纪要.md)
- **状态**：✅ 决策对齐 / ✅ 系统调研完成 / ⏳ PR-1 待启动

---

## 一、本文档定位

本文档基于路线图 + 多轮决策对齐后的**最终实施计划**。与路线图的关系：

| 路线图原方案 | 本计划调整 |
|---|---|
| WALL → FARM 改名（破坏 20 关）| **复制 WALL → FARM**（独立类型，20 关零影响）|
| 自动产兵按阶段分（PR-2 才移除）| **PR-1 一并全移除**（避免验证混乱）|
| 战前编制正常走 | **跳过战前编制**，固定 loadout |
| 指挥官让玩家选 | **强制 test_all**（绕过解锁 + 无 economy_boost）|
| 入口走测试关卡分类 | **主菜单加"T1 测试"快捷按钮** |
| 可建区视觉不显示 | **建造模式下显示**，设置菜单加开关 |
| 建造范围 6 格（未定义形状）| **圆形，半径 6 格 = 384 px** |
| 20 关回归测试 | **20 关冻结**，T1 期间不回归 |

---

## 二、决策汇总（实施前必读）

### 2.1 已确认决策

| # | 项 | 决策 | 理由 |
|---|---|---|---|
| D1 | FARM 实现 | **复制 WALL → 独立 PlaceMode.FARM** | 旧 20 关零回归风险 |
| D2 | 自动产兵 | **全移除**（BARRACKS/MONASTERY/ARCHERY_RANGE）| 避免 PR-1 验证混乱 |
| D3 | 建造范围形状 | **圆形，半径 6 格 = 384 px** | 实现简单，可建面积够用 |
| D4 | 玩家城堡位置 | 维持 (200, 500)，可建区被左边界裁切 | 先测起来，后续再调 |
| D5 | 可建区视觉 | **建造模式下显示主基地周围圆环**，设置开关默认关 | 不挡视野 |
| D6 | 范围外反馈 | 预览变红 + floating_text | 双反馈 |
| D7 | 战前编制 | **跳过界面**，用默认值（test_all 自带技能+变体）| 简化测试入口 |
| D8 | 指挥官 | **强制 test_all**（绕过解锁）| 无 economy_boost 干扰 |
| D9 | loadout | 固定 `[FARM, TOWER, BARRACKS, SOLDIER, ARCHER]` | T1 不开放其他兵种 |
| D10 | 主基地 | **缓存单个引用**，不允许造新主基地 | 简化可建区判定 |
| D11 | 入口 | **主菜单"T1 测试"快捷按钮** | 直达测试 |
| D12 | 农场位置 | 建造栏第 1 位（W+1）| 操作习惯 |
| D13 | 20 关 | **冻结**，T1 期间不维护 | 集中精力 |

### 2.2 已知债务（留给 T2+）

| 债务 | 来源 | 处理时机 |
|---|---|---|
| MONASTERY / ARCHERY_RANGE 失去自动产兵 | D2 全移除 | T2 阶段统一做主动触发机制 |
| 旧 20 关回归测试 | D13 冻结 | T1 完成后统一修 |
| 可建区被地图左边界裁切 | D4 维持位置 | 视测试体验决定是否调位置 |
| 玩家城堡 HP / 敌方城堡 HP 微调 | 暂用默认 | PR-3 验证骚扰节奏时调 |

---

## 三、PR 拆分（5 个 PR，递增可验证）

### 3.1 总览

| PR | 范围 | 工时 | 验收点 | 状态 |
|---|---|---|---|---|
| **PR-1** | 经济底层 + 核心产兵闭环 + 测试地图 | 6-8h | 农场造出来 + 兵营反兵 + 6 格可建区 | ⏳ 待启动 |
| **PR-2** | 操作端 UI 完善 | 3-4h | 动态造价显示 + 灰显 + 兵营队列 UI | ⏳ |
| **PR-3** | 骚扰波次 4 波 | 2-3h | 1:30 警告 → 2:00 出兵 → 路过打单位 | ⏳ |
| **PR-4** | SH1 据点（候选 C）| 6-8h | 占领光圈 + 高级弓箭手 + 分裂攻击 | ⏳ |
| **PR-5** | 最终验证 + 数值微调 | 2-3h | 三流程推演 + 4 波骚扰验证 + SH1 | ⏳ |

### 3.2 PR 之间的依赖

```
PR-1（核心闭环）─→ PR-2（UI 完善）─→ PR-3（骚扰）─→ PR-5（验证）
       │
       └─→ PR-4（据点）────────────────────────────────┘
```

- PR-1 是所有后续 PR 的依赖
- PR-2 和 PR-4 可并行（如果有多人）
- PR-3 不依赖 PR-2（骚扰配置与 UI 独立）

---

## 四、PR-1 详细计划（当前阶段）

### 4.1 改动文件清单

#### 代码层（11 个）

| # | 文件 | 改动 |
|---|---|---|
| 1 | [building_stats.gd](../../scripts/stats/building_stats.gd) | 加 5 字段：cost_increment / completion_refund / completion_refund_unit / completion_refund_unit_count / gold_production_amount |
| 2 | [game_data.gd](../../scripts/systems/game_data.gd) | enum PlaceMode 加 FARM；COSTS / MODE_NAMES / BUILDING_SCENES 加 FARM 项 |
| 3 | [building.gd](../../scripts/buildings/building.gd) | enum BuildingType 加 FARM；移除 _production_process 自动产兵；_produce_gold 读 stats；_setup_texture 给 FARM 配贴图（复用 House1.png）|
| 4 | [building_placer.gd](../../scripts/systems/building_placer.gd) | get_current_cost / 完成反还 hook / is_in_buildable_area / 农场上限 + 兵营解锁 / 预览变红 + show_build_range 开关 |
| 5 | [game_ui.gd](../../scripts/systems/game_ui.gd) | 暂不动（PR-2 做）|
| 6 | [main.gd](../../scripts/main.gd) | 缓存主基地引用 / 读 temp_flag 跳编制 + 强制 test_all + 固定 loadout / 加载 show_build_range 设置 |
| 7 | [save_manager.gd](../../scripts/systems/save_manager.gd) | 新增 `_temp_flags` 字典 + set/get/clear_temp_flag 方法（会话级，不持久化）|
| 8 | [main_menu.gd](../../scripts/ui/main_menu.gd) | 加"T1 测试"按钮 + 入口回调；settings 加"显示建造范围"CheckButton |
| 9 | [commander_choice.gd](../../scripts/commander/commander_choice.gd) | 暂不动（`set_pending_level` / `player_selected_id` 已有）|
| 10 | [load_router.gd](../../scripts/systems/load_router.gd) | 暂不动（`request_load` 已有，复用）|
| 11 | [translations.csv](../../locales/translations.csv) | ENTITY_FARM + NO_BARRACKS / QUEUE_FULL / OUT_OF_RANGE 等键 |

#### 资源层（新建 4 个）

| # | 文件 | 内容 |
|---|---|---|
| 11 | `resources/stats/buildings/farm_stats.tres` | 复制 wall_stats.tres 改：id=&"farm", building_type=6, is_gold_producer=true, gold_production_amount=20, cost_increment=150, max_hp=150, build_time=20.0 |
| 12 | `scenes/buildings/farm.tscn` | 复制 wall.tscn 改：building_type=FARM，stats=farm_stats.tres |
| 13 | `resources/map_t1_economy_test_config.tres` | 测试关卡配置（initial_gold=400, balance_scheme=2 等）|
| 14 | `scenes/maps/map_t1_economy_test.tscn` | 测试关卡场景（玩家/敌方城堡 + SH1 守军 + WaveManager 占位）|

#### 资源层（修改 2 个）

| # | 文件 | 改动 |
|---|---|---|
| 15 | [barracks_stats.tres](../../resources/stats/buildings/barracks_stats.tres) | 加 completion_refund_unit=&"soldier", completion_refund_unit_count=2 |
| 16 | [castle_stats.tres](../../resources/stats/buildings/castle_stats.tres) | 加 gold_production_amount=50 |

### 4.2 关键实现要点

#### A. 复制 WALL → FARM（不要破坏 WALL）

**关键点**：所有改动**新增 FARM 项**，不要修改 WALL 的任何代码。

```gdscript
# game_data.gd
enum PlaceMode { NONE, WALL, TOWER, CASTLE, BARRACKS, ..., FARM }  # FARM 接在最后
const COSTS := { ..., PlaceMode.FARM: 100 }
const MODE_NAMES := { ..., PlaceMode.FARM: "ENTITY_FARM" }
const BUILDING_SCENES := { ..., BuildingType.FARM: "res://scenes/buildings/farm.tscn" }

# building.gd
enum BuildingType { WALL, TOWER, CASTLE, BARRACKS, MONASTERY, ARCHERY, FARM }
```

#### B. 移除自动产兵（全移除，但保留字段）

```gdscript
# building.gd _production_process - 移除自动 spawn 调用
# 保留 production_variant_ids 字段（T2 给 MONASTERY/ARCHERY 主动触发用）
# BARRACKS 改为：玩家下单 → production_queue 排队 → 按 cooldown 出兵
```

#### C. 可建区判定（圆形 + 半径 6 格）

```gdscript
# building_placer.gd
const BUILD_RADIUS := 6 * 64  # 384 px

func is_in_buildable_area(pos: Vector2) -> bool:
    if main_node.player_castle == null: return false
    return pos.distance_to(main_node.player_castle.position) <= BUILD_RADIUS
```

#### D. 兵营出兵队列（最小实现）

```gdscript
# building.gd BARRACKS 实例
var production_queue: Array[StringName] = []
const QUEUE_MAX := 5

func queue_has_space() -> bool:
    return production_queue.size() < QUEUE_MAX

func queue_unit(unit_id: StringName) -> void:
    production_queue.append(unit_id)

# main.gd _do_place 改：
# if D.is_unit_mode(place_mode):
#     var barracks = _find_nearest_available_barracks(unit_type, click_pos)
#     if barracks == null: ui.show_hint("NO_BARRACKS"); return
#     if not barracks.queue_has_space(): ui.show_hint("QUEUE_FULL"); return
#     barracks.queue_unit(unit_id)
#     gold -= cost
```

#### E. 强制 test_all + 跳过编制（在 main_menu 按钮逻辑）

**前置依赖**：在 [save_manager.gd](../../scripts/systems/save_manager.gd) 加 `_temp_flags: Dictionary` + set/get_temp_flag 方法（不持久化）。

```gdscript
# main_menu.gd "T1 测试" 按钮回调
func _on_t1_test_pressed() -> void:
    SaveManager.set_temp_flag("skip_loadout_screen", true)
    SaveManager.set_temp_flag("force_commander", &"test_all")
    CommanderChoice.set_pending_level(
        "res://scenes/maps/map_t1_economy_test.tscn", "t1_test")
    LoadRouter.request_load("res://scripts/main.tscn", false)  # 复用现有异步加载入口

# main.gd 启动初始化（约 112 行附近）
func _ready():
    if SaveManager.get_temp_flag("skip_loadout_screen", false):
        CommanderChoice.player_selected_id = SaveManager.get_temp_flag("force_commander")
        _selected_loadout = [
            D.PlaceMode.FARM, D.PlaceMode.TOWER, D.PlaceMode.BARRACKS,
            D.PlaceMode.SOLDIER, D.PlaceMode.ARCHER
        ]
        SaveManager.clear_temp_flags()  # 用完清理
    else:
        _selected_loadout = load_player_loadout()  # 走原流程
```

### 4.3 PR-1 验收清单

进入 T1 测试关卡（主菜单按钮），逐项验证：

#### 经济数值
- [ ] 开局 400 金（NORMAL 难度）
- [ ] 主基地每 10s +50 金（取代硬编码 30）
- [ ] 按 W+1 → 进入 FARM 建造模式
- [ ] 第 1 个农场造价 100，第 2 个 250，第 3 个 400，第 4 个 550，第 5 个 700
- [ ] 农场造价达 5 个后无法再建（灰显或 floating_text 提示）
- [ ] 农场建造 20s 完成
- [ ] 农场每 10s +20 金

#### 兵营 + 产兵
- [ ] 没农场时按 W+3（BARRACKS）→ 灰显或提示"需要农场"
- [ ] 造 1 农场后按 W+3 → 可建造，造价 300
- [ ] 兵营 5s 完成 → **反 2 个 soldier** 在兵营门口生成
- [ ] 按 Q+1（SOLDIER）→ 点击地图 → 找最近兵营入队 + 扣 50 金
- [ ] 没兵营时按 Q+1 → 提示"没有可用兵营"，不扣金
- [ ] 兵营队列满 5 时按 Q+1 → 提示"队列已满"
- [ ] 兵营按 cooldown 顺序出兵

#### 可建区
- [ ] 主基地周围 6 格内可建造（圆形）
- [ ] 范围外点击建造 → 预览变红 + floating_text"范围外"
- [ ] 设置菜单"显示建造范围"开关切换有效（默认关）
- [ ] 开关开时，建造模式下显示半透明绿色圆环

#### 操作流程
- [ ] 主菜单"T1 测试"按钮 → 直接进游戏（不弹指挥官/编制界面）
- [ ] 进入后是 test_all 指挥官（无 economy_boost 干扰）
- [ ] 玩家默认技能 = [0,1,2,3]（ORBITAL_STRIKE / HEAL_FIELD / SHIELD_WALL / UNIT_DROP）

#### 旧 20 关（冻结验证）
- [ ] 旧 20 关暂不可进入（隐藏入口）
- [ ] 或可进入但接受 WALL/garrison 等机制混乱

---

## 五、PR-2~5 摘要（启动时再细化）

### PR-2 操作端 UI 完善（3-4h）

- 建造栏图标右下角动态造价显示
- 农场上限灰显 / 兵营解锁灰显
- 兵营头顶队列 UI（小图标 + 倒计时）
- PLACE_BUILDING 文案显示当前造价
- 翻译键补全

### PR-3 骚扰波次（2-3h）

- 新增 `elite_vanguard` 占位 stats
- WaveManager 在测试关卡场景注入（4 波配置）
- 1:30 警告 + 2:00 出兵 + 路径 ~18s 接敌
- 警告 UI（objectives_panel 或 floating_text，启动时决策）
- 骚扰单位 attack_move 到玩家城堡

**待决策**（Q-IMP6/7/8，PR-3 启动前定）：
- 警告 UI 选型
- WaveManager 注入方式（场景节点 vs 代码生成）
- spawn 位置分布（圆形 vs 随机点）

### PR-4 SH1 据点（6-8h，最复杂）

- 占领光圈系统
- `outpost_captured` 信号 + 范围内敌方清空检测
- 据点建筑栏 UI（占领后解锁）
- 候选 C：高级弓箭手祭坛建筑
- 高级弓箭手单位（分裂攻击，同时打 3 目标）
- 据点归属判定

### PR-5 最终验证（2-3h）

- 三流程推演（贪农场 / 稳一手 / 新手延迟）
- 4 波骚扰节奏验证
- SH1 占领验证
- 数值微调

---

## 六、T1 测试关卡规格（PR-1 实施）

### 6.1 基本信息

| 项 | 值 |
|---|---|
| 场景 | `scenes/maps/map_t1_economy_test.tscn` |
| 配置 | `resources/map_t1_economy_test_config.tres` |
| 地图尺寸 | `Rect2(0, 0, 1500, 1000)` |
| 玩家城堡位置 | `(200, 500)` |
| 敌方城堡位置 | `(1300, 500)` |
| SH1 据点位置 | `(750, 500)`（PR-4 才实装）|
| balance_scheme | 2（SLOW）|
| initial_gold | 400 |

### 6.2 玩家初始

```gdscript
player_buildings = [
    {"type": "CASTLE", "grid_pos": Vector2i(3, 8)}
]
player_units = [
    {"type": "SOLDIER", "pos": Vector2(150, 450)},
    {"type": "SOLDIER", "pos": Vector2(150, 550)}
]
available_items = [FARM, TOWER, BARRACKS, SOLDIER, ARCHER]  # 实际由 force_loadout 覆盖
```

### 6.3 敌方初始

```gdscript
enemy_buildings = [
    {"type": "CASTLE", "grid_pos": Vector2i(28, 8)}  # HP 调高到 1500
]
enemy_units = []  # 由 WaveManager 调度
```

### 6.4 SH1 占位（PR-4 才实装）

PR-1 阶段 SH1 区域**只放守军**，不挂 OutpostCommander：

```gdscript
# SH1 守军（场景启动时生成，但据点机制 PR-4 才做）
enemy_units += [
    {"type": "SOLDIER", "pos": Vector2(720, 470)},
    {"type": "SOLDIER", "pos": Vector2(720, 530)},
    {"type": "SOLDIER", "pos": Vector2(780, 500)},
    {"type": "ARCHER", "pos": Vector2(750, 460)}
]
enemy_buildings += [
    {"type": "TOWER", "grid_pos": Vector2i(14, 7)}  # SH1 内箭塔
]
```

### 6.5 WaveManager 占位（PR-3 才配置）

PR-1 阶段**不注入 WaveManager**，让玩家先验证经济数值。PR-3 启动时再加。

---

## 七、待决策清单（按 PR 分组）

### 7.1 PR-1 调研结论（已敲定）

#### Q-1：SaveManager 临时 flag 机制

**现状**：不存在 temp_flag 机制。SaveManager 只做持久化（`user://saves/slot_%d.json`）。

**相关 API（[save_manager.gd](../../scripts/systems/save_manager.gd)）**：
- `enter_default_slot()` / `set_current_slot(slot)` / `get_current_data()`
- `is_commander_unlocked(cmd_id)` / `unlock_commander(cmd_id)`
- `set_last_selected_commander(cmd_id)` / `get_last_selected_commander()`
- `start_game_session(level_id)` / `end_game_session(result, difficulty)`

**CommanderChoice（[commander_choice.gd](../../scripts/commander/commander_choice.gd)）**：
- `player_selected_id: StringName = &"balanced"`（核心字段）
- `select_commander()`（UI 写入）
- `set_pending_level(scene_path, level_id)`（关卡路径传递）
- main.gd:186 读 `player_selected_id`；main.gd:112 读 loadout

**map_config 注入路径**：main.gd:11 `@export var map_config`，场景实例化时由外部注入；没有"指定路径直接进入"API。

**LoadRouter（[load_router.gd](../../scripts/systems/load_router.gd)）**：
- `LoadRouter.request_load(scene_path, is_multiplayer)` —— 现成的异步场景加载入口（含进度条），可复用。

**PR-1 设计方案**：在 [save_manager.gd](../../scripts/systems/save_manager.gd) 新增会话级临时存储：
```gdscript
var _temp_flags: Dictionary = {}  # 不持久化，仅本会话有效

func set_temp_flag(key: String, value: Variant) -> void:
    _temp_flags[key] = value

func get_temp_flag(key: String, default: Variant = null) -> Variant:
    return _temp_flags.get(key, default)

func clear_temp_flags() -> void:
    _temp_flags.clear()
```

**T1 测试入口流程**（复用现有路径 + 最小改动）：
```
主菜单"T1 测试"按钮
  → SaveManager.set_temp_flag("skip_loadout_screen", true)
  → SaveManager.set_temp_flag("force_commander", "test_all")
  → CommanderChoice.set_pending_level("res://scenes/maps/map_t1_economy_test.tscn", "t1_test")
  → LoadRouter.request_load("res://scripts/main.tscn", false)
main.gd 启动时:
  → if SaveManager.get_temp_flag("skip_loadout_screen", false):
        CommanderChoice.player_selected_id = SaveManager.get_temp_flag("force_commander")
        _selected_loadout = [FARM, TOWER, BARRACKS, SOLDIER, ARCHER]
        跳过 commander_select 界面
```

#### Q-2：Settings 系统

**现状**：完善。ConfigFile 存储 + `user://settings.cfg`，UI 动态创建于 [main_menu.gd](../../scripts/ui/main_menu.gd) 的 `_show_settings_overlay()`（行 367-548）。

**现有 sections**：`[display]` / `[audio]` / `[game]` / `[gameplay]` / `[commander_skills]` / `[loadout]` / `[passives]` / `[save]`

**现有 CheckButton 开关**（可参考）：
- FPS 显示（main_menu.gd:484）
- 寻路路径线（main_menu.gd:521）
- 碰撞区域显示（main_menu.gd:535）

**写设置 API**（main_menu.gd:639）：
```gdscript
func _save_setting(section: String, key: String, value) -> void:
    var config := ConfigFile.new()
    config.load(SETTINGS_PATH)
    config.set_value(section, key, value)
    config.save(SETTINGS_PATH)
```

**立即生效机制**：toggle 回调里直接改全局变量（如 `Unit.show_path_lines = pressed`）。

**PR-1 设计方案**：在 `[game]` section 加 `show_build_range` 开关：

| 改动位置 | 内容 |
|---|---|
| main_menu.gd `_show_settings_overlay()` 约 535 行后 | 加新 HBoxContainer + Label "显示建造范围" + CheckButton，默认 false |
| main_menu.gd 回调区 | 加 `_on_settings_build_range_toggled(pressed)` → `_save_setting("game", "show_build_range", pressed)` |
| main.gd 加载设置（约 1526 行附近） | `building_placer.show_build_range = config.get_value("game", "show_build_range", false)` |
| building_placer.gd | 新增 `var show_build_range: bool` 控制圆环可见性 |

#### Q-3：主菜单"T1 测试"按钮

**现状**：[main_menu.gd](../../scripts/ui/main_menu.gd) 当前 5 个按钮（开始游戏 / 切换存档 / 多人游戏 / 设置 / 退出），无现成开发者测试入口。

**入口流程对比**：
```
普通关卡：主菜单 → 开始游戏 → enter_default_slot → level_select.tscn
         → 选关 → set_pending_level → commander_select.tscn
         → 选指挥官/loadout → LoadRouter.request_load(main.tscn)
```

**PR-1 设计方案**：在 main_menu.gd:174 后加第 6 个按钮"T1 测试"，回调：

```gdscript
func _on_t1_test_pressed() -> void:
    SaveManager.set_temp_flag("skip_loadout_screen", true)
    SaveManager.set_temp_flag("force_commander", &"test_all")
    CommanderChoice.set_pending_level(
        "res://scenes/maps/map_t1_economy_test.tscn", "t1_test")
    LoadRouter.request_load("res://scripts/main.tscn", false)
```

main.gd 启动时检查 temp_flag，决定是否跳过 commander_select 流程。**无需新建场景入口，完全复用 LoadRouter**。

### 7.2 PR-3 待决策（启动前定）

| 编号 | 问题 | 推荐 |
|---|---|---|
| Q-IMP6 | 警告 UI 选型 | objectives_panel 倒计时（已有系统）|
| Q-IMP7 | WaveManager 注入方式 | 场景节点（在 .tscn 里配置）|
| Q-IMP8 | spawn 位置分布 | 敌方城堡周围圆形分布 |

### 7.3 PR-4 待决策（启动前定）

| 编号 | 问题 | 推荐 |
|---|---|---|
| Q-SH1 | 高级弓箭手分裂目标数 | 3 个 |
| Q-SH2 | 据点建筑栏 UI 位置 | 建造栏顶部新增"据点"分类 |
| Q-SH3 | 占领光圈视觉 | 地面发光圆环 + 中心光柱 |

---

## 八、风险与应对

| 风险 | 概率 | 应对 |
|---|---|---|
| test_all 没解锁导致进入失败 | 高 | 强制覆盖时绕过 is_commander_unlocked 检查 |
| 复制 FARM 时漏改 stats 字段 | 中 | 实施时对照 wall_stats.tres 逐字段核对 |
| 移除自动产兵后旧关 MONASTERY/ARCHERY 报错 | 中 | 已记录为 T2 债务，T1 期间旧关冻结 |
| 可建区被左边界裁切影响测试 | 中 | 接受现状，PR-5 验证时再决定是否调整 |
| 主菜单按钮入口路径错误 | 中 | 复用现有"快速进入关卡"逻辑（如有）|

---

## 九、下一步

1. **本文档确认**：用户确认决策无误
2. **系统调研**：✅ 完成（Q-1/Q-2/Q-3 结论见 7.1 节）
3. **更新路线图**：在 [T1实施路线图.md](T1实施路线图.md) 顶部加链接指向本文档，标注"以本计划为准"
4. **启动 PR-1**：按 4.1 清单逐文件实施
5. **PR-1 验证**：按 4.3 清单逐项验证

---

## 附录：与原路线图的差异表

| 路线图原条目 | 本计划调整 | 原因 |
|---|---|---|
| 2.2 坑 #2「WALL→FARM 影响 20 关」| 删除（复制方案无此问题）| D1 |
| 2.2 坑 #6「building.gd 产金硬编码 30」| 保留 | 仍需改 |
| 3.7 指挥官选择推荐 test_all | 改为**强制 test_all** | D8 |
| 4.2 阶段 0.2「建造范围」| 移到 PR-1（不再单独阶段）| 范围调整 |
| 4.3 阶段 1 清单 | 扩展（+移除自动产兵 +兵营出兵队列）| D2 |
| 4.4 阶段 2「移除自动产兵」| 移到 PR-1 | D2 |
| 4.7 阶段 5「20 关 initial_gold 批量改」| 删除（冻结）| D13 |
| 5.3 PR-1 范围 | 扩展（包含部分原 PR-2 内容）| D2 |
| 6.5 回归测试 | 删除（冻结）| D13 |
| 8.1 Q-IMP1「新建 commander_test_basic.tres」| 删除（用 test_all 即可）| D8 |
